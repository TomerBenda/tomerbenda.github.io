name = "tbd-view-counter"
main = "view-counter-worker.js"
compatibility_date = "2025-01-01"

# Daily snapshot of total reads for the stats dashboard's over-time chart
[triggers]
crons = ["0 4 * * *"]

[[kv_namespaces]]
binding = "COUNTS"
id = "tbd-blog-view-counts"
